
> Perform a complete code restructure and cleanup across the entire project. Do everything in this exact order:
>
> ---
>
> ## 1. Fix app/globals.css
>
> Rewrite the file completely in this exact order:
>
> ```css
> @import "tailwindcss";
> @import "tw-animate-css";
> @import "shadcn/tailwind.css";
>
> @custom-variant dark (&:is(.dark *));
>
> @theme inline {
>   --font-sans: var(--font-sans);
>   --font-heading: var(--font-sans);
>   /* keep all shadcn @theme tokens exactly as they are */
> }
>
> :root {
>   /* Our custom tokens */
>   --bg-primary: #ffffff;
>   --bg-secondary: #f5f5f5;
>   --bg-card: #efefef;
>   --text-primary: #0a0a0a;
>   --text-secondary: #444444;
>   --text-muted: #888888;
>   --custom-border: #e0e0e0;
>   --lime: #CCFF00;
>
>   /* shadcn tokens — keep all exactly as shadcn generated */
>   --background: oklch(1 0 0);
>   --foreground: oklch(0.145 0 0);
>   /* ... rest of shadcn tokens unchanged */
> }
>
> .dark {
>   /* Our custom tokens */
>   --bg-primary: #0a0a0a;
>   --bg-secondary: #111111;
>   --bg-card: #1a1a1a;
>   --text-primary: #f5f5f5;
>   --text-secondary: #a3a3a3;
>   --text-muted: #555555;
>   --custom-border: #222222;
>   --lime: #CCFF00;
>
>   /* shadcn tokens — keep all exactly as shadcn generated */
>   --background: oklch(0.145 0 0);
>   /* ... rest of shadcn tokens unchanged */
> }
> ```
>
> Key changes:
> - Rename our `--border` to `--custom-border` to avoid shadcn conflict
> - Rename our `--accent` to `--lime` to avoid shadcn conflict
> - Keep all shadcn tokens untouched
> - One `body` block only
> - Remove `@layer base` block entirely
> - Keep hero-noise, scrollbar, selection styles
>
> ---
>
> ## 2. Create folder structure
>
> Create these folders and files:
>
> ```
> lib/
>   constants/
>     navigation.ts
>   utils/
>     scroll.ts
>   i18n/
>     config.ts
>     locales/
>       en/
>         common.json
>         hero.json
>         navbar.json
>         footer.json
> hooks/
>   useTranslation.ts
>   useScrollSection.ts
> components/
>   ui/          ← already exists from shadcn
>   sections/
>     Hero.tsx   ← move from components/Hero.tsx
>   layout/
>     Navbar.tsx ← move from components/Navbar.tsx
>     Footer.tsx ← move from components/Footer.tsx
> ```
>
> ---
>
> ## 3. lib/constants/navigation.ts
>
> ```typescript
> /**
>  * Navigation items for the main menu.
>  * Each item maps a display label to a section id.
>  */
> export interface NavItem {
>   /** Display label shown in the navbar */
>   label: string;
>   /** DOM id of the target section */
>   id: string;
> }
>
> /** Primary navigation items used in Navbar and Footer */
> export const NAV_ITEMS: NavItem[] = [
>   { label: 'INTRO', id: 'intro' },
>   { label: 'ABOUT', id: 'about' },
>   { label: 'PROJECTS', id: 'projects' },
>   { label: 'SKILLS', id: 'skills' },
>   { label: 'JOURNEY', id: 'journey' },
>   { label: 'CONTACT', id: 'contact' },
> ] as const;
>
> /** Social links used in Footer and Hero */
> export const SOCIAL_LINKS = {
>   email: 'mailto:samdsiva1994@gmail.com',
>   linkedin: 'https://linkedin.com/in/sam-dhanalakota-946649147',
>   github: 'https://github.com/samdhanalakota',
> } as const;
> ```
>
> ---
>
> ## 4. lib/utils/scroll.ts
>
> ```typescript
> /**
>  * Smoothly scrolls the page to a section by its DOM id.
>  * @param id - The id attribute of the target section element
>  */
> export function scrollToSection(id: string): void {
>   const target = document.getElementById(id);
>   if (!target) {
>     console.warn(`[scrollToSection] No element found with id: "${id}"`);
>     return;
>   }
>   target.scrollIntoView({ behavior: 'smooth', block: 'start' });
> }
> ```
>
> ---
>
> ## 5. lib/i18n/locales/en/hero.json
>
> ```json
> {
>   "label": "SAM DHANALAKOTA — SENIOR FULL-STACK ENGINEER",
>   "headline_1": "Building AI-native",
>   "headline_2": "products with React",
>   "headline_3": "& Node.",
>   "subparagraph": "Full-stack engineer crafting secure, high-performance systems for enterprise teams — powered by AI.",
>   "cta_primary": "VIEW WORK",
>   "cta_secondary": "GET IN TOUCH",
>   "scroll_label": "SCROLL"
> }
> ```
>
> ## 6. lib/i18n/locales/en/navbar.json
>
> ```json
> {
>   "logo_alt": "Sam Dhanalakota logo",
>   "toggle_theme": "Toggle theme",
>   "open_menu": "Open mobile menu",
>   "close_menu": "Close mobile menu",
>   "nav_items": {
>     "intro": "INTRO",
>     "about": "ABOUT",
>     "projects": "PROJECTS",
>     "skills": "SKILLS",
>     "journey": "JOURNEY",
>     "contact": "CONTACT"
>   }
> }
> ```
>
> ## 7. lib/i18n/locales/en/footer.json
>
> ```json
> {
>   "tagline": "Building enterprise platforms that scale.",
>   "quick_links_heading": "Quick Links",
>   "contact_heading": "Contact",
>   "email_label": "samdsiva1994@gmail.com",
>   "linkedin_label": "LinkedIn",
>   "github_label": "GitHub",
>   "message_label": "Leave a Message",
>   "copyright": "© 2026 Sam Dhanalakota. All rights reserved.",
>   "back_to_top": "Back to top"
> }
> ```
>
> ## 8. lib/i18n/config.ts
>
> ```typescript
> /**
>  * Lightweight translation utility that mimics the i18next API.
>  * Supports dot-notation key access into locale JSON files.
>  * Easy to swap for react-i18next when multi-language support is needed.
>  */
>
> import heroEn from './locales/en/hero.json';
> import navbarEn from './locales/en/navbar.json';
> import footerEn from './locales/en/footer.json';
>
> /** Supported namespaces */
> export type Namespace = 'hero' | 'navbar' | 'footer';
>
> /** All locale data keyed by namespace */
> const locales: Record<Namespace, Record<string, unknown>> = {
>   hero: heroEn,
>   navbar: navbarEn,
>   footer: footerEn,
> };
>
> /**
>  * Returns a translation function for the given namespace.
>  * @param ns - The namespace to load translations from
>  * @returns A function t(key) that returns the translated string
>  *
>  * @example
>  * const t = getTranslations('hero');
>  * t('headline_1') // "Building AI-native"
>  */
> export function getTranslations(ns: Namespace) {
>   const data = locales[ns];
>   return function t(key: string): string {
>     const value = data[key];
>     if (typeof value === 'string') return value;
>     console.warn(`[i18n] Missing translation for key: "${key}" in namespace: "${ns}"`);
>     return key;
>   };
> }
> ```
>
> ## 9. hooks/useTranslation.ts
>
> ```typescript
> /**
>  * Client-side translation hook.
>  * Mimics the react-i18next useTranslation hook API.
>  * Can be swapped for the real hook when multi-language support is added.
>  *
>  * @param ns - Namespace to load translations from
>  * @returns Object containing the t() translation function
>  *
>  * @example
>  * const { t } = useTranslation('hero');
>  * t('headline_1') // "Building AI-native"
>  */
> 'use client';
>
> import { getTranslations, Namespace } from '@/lib/i18n/config';
>
> export function useTranslation(ns: Namespace) {
>   const t = getTranslations(ns);
>   return { t };
> }
> ```
>
> ## 10. hooks/useScrollSection.ts
>
> ```typescript
> /**
>  * Hook that tracks which section is currently visible in the viewport.
>  * Uses IntersectionObserver for performance.
>  *
>  * @param sectionIds - Array of section ids to observe
>  * @returns The id of the currently visible section
>  *
>  * @example
>  * const activeSection = useScrollSection(['intro', 'about', 'projects']);
>  */
> 'use client';
>
> import { useEffect, useState } from 'react';
>
> export function useScrollSection(sectionIds: string[]): string {
>   const [activeSection, setActiveSection] = useState<string>(sectionIds[0] ?? '');
>
>   useEffect(() => {
>     const observer = new IntersectionObserver(
>       (entries) => {
>         const visible = entries
>           .filter((e) => e.isIntersecting)
>           .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
>
>         if (visible) setActiveSection(visible.target.id);
>       },
>       {
>         threshold: [0.25, 0.5, 0.75],
>         rootMargin: '-35% 0px -45% 0px',
>       }
>     );
>
>     sectionIds.forEach((id) => {
>       const el = document.getElementById(id);
>       if (el) observer.observe(el);
>     });
>
>     return () => observer.disconnect();
>   }, [sectionIds]);
>
>   return activeSection;
> }
> ```
>
> ---
>
> ## 11. Update all components to use new imports
>
> **components/layout/Navbar.tsx:**
> - Move file from components/Navbar.tsx to components/layout/Navbar.tsx
> - Import NAV_ITEMS from '@/lib/constants/navigation'
> - Import scrollToSection from '@/lib/utils/scroll'
> - Import useScrollSection from '@/hooks/useScrollSection'
> - Use useTranslation('navbar') for all strings
> - Replace inline NAV_ITEMS array with imported constant
> - Replace inline scrollToSection function with imported utility
> - Replace IntersectionObserver logic with useScrollSection hook
> - Active section highlight color: #CCFF00 (lime) instead of var(--text-primary)
> - Replace all --border references with --custom-border
> - Replace all --accent references with --lime
> - Add JSDoc comment above component
>
> **components/layout/Footer.tsx:**
> - Move file from components/Footer.tsx to components/layout/Footer.tsx
> - Import NAV_ITEMS, SOCIAL_LINKS from '@/lib/constants/navigation'
> - Import scrollToSection from '@/lib/utils/scroll'
> - Use useTranslation('footer') for all strings
> - Replace all --border with --custom-border
> - Add JSDoc comment above component
>
> **components/sections/Hero.tsx:**
> - Move file from components/Hero.tsx to components/sections/Hero.tsx
> - Use useTranslation('hero') for all strings
> - Replace all --border with --custom-border
> - Replace all --accent with --lime
> - Import scrollToSection from '@/lib/utils/scroll'
> - Add JSDoc comment above component
> - Fix label size: increase to text-sm (14px)
> - Fix headline color: use text-[#F5F5F5] instead of white (softer)
> - Fix sub paragraph width: use w-[55%] instead of fixed max-w
> - Fix buttons: use shadcn Button component imported from '@/components/ui/button'
>   - Primary button: variant with bg-[#CCFF00] text-black
>   - Secondary button: variant="outline"
>   - Both: size="lg" rounded-full
> - Fix button alignment: flex-row items-center gap-4 mt-8
>
> **components/CustomCursor.tsx and components/ScrollProgressBar.tsx:**
> - Replace --accent with --lime
> - Replace --border with --custom-border
> - Add JSDoc comments
>
> **app/layout.tsx:**
> - Update Navbar import to '@/components/layout/Navbar'
> - Keep everything else the same
>
> **app/page.tsx:**
> - Update Hero import to '@/components/sections/Hero'
> - Update Footer import to '@/components/layout/Footer'
>
> ---
>
> ## 12. Delete old files
> - Delete components/Hero.tsx (moved to sections/)
> - Delete components/Navbar.tsx (moved to layout/)
> - Delete components/Footer.tsx (moved to layout/)
>
> ---
>
> Show me a summary of all files created/moved/updated when done.

---